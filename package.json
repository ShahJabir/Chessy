{
  "name": "chess-arena",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "CHESS ARENA — a cinematic 3D chess game where every move changes the battlefield.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "three": "^0.169.0"
  },
  "devDependencies": {
    "@types/three": "^0.169.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.14",
    "vitest": "^2.1.9"
  }
}
